sheets/
=======

Drop the three exported sheet PNGs here:

  2D-01-topology-node-families.png      (1680 × 1180 source)
  3D-01-hardware-primitive-family.png   (1680 × 1300 source)
  IXN-01-interaction-storyboard.png     (1840 × 1100 source)

These are reference exports only — the canonical source is the inline-SVG
.jsx files under ../source/. OCC implementers should regenerate these
from the React source rather than treat the PNGs as truth.
