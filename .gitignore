# --- Node / Vite / pnpm ---
node_modules/
dist/
dist-ssr/
.vite/
*.local
.eslintcache
.pnpm-store/

# --- Tauri / Rust ---
src-tauri/target/
src-tauri/gen/
**/*.rs.bk
Cargo.lock.bak

# --- Editor / OS ---
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db
*.swp

# --- Logs / env ---
*.log
npm-debug.log*
pnpm-debug.log*
.env
.env.*.local

# --- AgentOps (.agents/ is a tracked markdown wiki — ignore only runtime state) ---
# Keep tracked: .agents/README.md, decisions/, findings/, learnings/, patterns/,
#   planning-rules/, runs/, council/, handoff/ — these are version-controlled.
# Ignore: daemon activation, caches, scratch, logs.
.agents/daemon/
.agents/cache/
.agents/.cache/
.agents/tmp/
.agents/**/*.tmp
.agents/**/*.log

# --- Graphify generated artifacts ---
graphify-out/

# --- Build / scratch ---
build/
out/
coverage/

# --- Operator Python venv (tooling only, NOT product runtime) ---
.venv/
__pycache__/
*.pyc
.pytest_cache/

# AgentOps session artifacts

# AgentOps session artifacts

# Local Claude runtime settings
.claude/

# --- AgentOps churn / runtime / auto-generated state ---
.agents/ao/
.agents/knowledge/
.agents/pool/
.agents/defrag/
.agents/signals/
.agents/handoff/stop-*.md
.agents/handoff/auto-*.json
.agents/learnings/2026-*.md
.agents/**/*.jsonl

# AgentOps session artifacts
/.agents/

# AgentOps session artifacts
.agents/

# Local Obsidian workspace state
obsidian/.obsidian/

# claudestat auto-generated handoff (local tool, not repo artifact)
HANDOFF.md

# --- Local agent working surfaces (V1AI-A hygiene) ---
# Codex's local working tree — never tracked.
.codex/
# Stray Windows redirect artifact.
nul
