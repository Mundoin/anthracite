# --- Node / Vite / pnpm ---
node_modules/
dist/
dist-ssr/
.vite/
*.local
.eslintcache
.pnpm-store/

# --- Tauri / Rust ---
src-tauri/target/
src-tauri/gen/
**/*.rs.bk
Cargo.lock.bak

# --- Editor / OS ---
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db
*.swp

# --- Logs / env ---
*.log
npm-debug.log*
pnpm-debug.log*
.env
.env.*.local

# AgentOps / AO runtime state
# AO owns this directory locally. Durable knowledge is promoted into tracked
# docs/operations/, obsidian/, AGENTS.md, CLAUDE.md, GOALS.md, or PRODUCT.md.
/.agents/
.agents/

# --- Graphify generated artifacts ---
graphify-out/

# --- Build / scratch ---
build/
!src/modes/build/
out/
coverage/

# --- Operator Python venv (tooling only, NOT product runtime) ---
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Local Claude runtime settings
.claude/

# Local Obsidian workspace state
obsidian/.obsidian/

# claudestat auto-generated handoff (local tool, not repo artifact)
HANDOFF.md

# --- Local agent working surfaces (V1AI-A hygiene) ---
# Codex's local working tree — never tracked.
.codex/
# Stray Windows redirect artifact.
nul
