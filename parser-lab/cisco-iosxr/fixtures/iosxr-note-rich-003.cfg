! synthetic fixture
! no customer data
! purpose: IOS-XR management-plane and note-rich parser prep
! feature coverage tags: hostname, routed-interface, loopback, static-route, management-plane, note-only markers
hostname xr-notes-003
!
interface Loopback0
 description svc-loopback
 ipv4 address 192.0.2.30 255.255.255.255
!
interface TenGigE0/0/0/0
 description core-wan
 ipv4 address 198.51.100.6 255.255.255.252
 no shutdown
!
interface TenGigE0/0/0/1
 description backup-wan
 ipv4 address 198.51.100.10 255.255.255.252
 shutdown
!
router static
 address-family ipv4 unicast
  0.0.0.0/0 198.51.100.5
  192.0.2.0/24 198.51.100.9
 !
!
ssh server v2
netconf-yang agent ssh
snmp-server community noc RO
ntp server 192.0.2.53
logging host 203.0.113.60
! note-only: acl markers would include route-policy and access-control constructs.
! note-only: nat markers would include translation constructs.
! note-only: qos markers would include policy-map and class-map constructs.
! note-only: aaa markers would include local-user and authentication blocks.
! note-only: security markers would include zoning or service-policy constructs.
! note-only: routing markers would include ospf/bgp/isis process blocks.

