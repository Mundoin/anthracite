# 010-replace-mode-warning expected review outcome

V1AS should show the same projected-edge surface, but the simulator summary must keep the replace warning visible.

- If the import is blocked, the review surface should stay unchanged and honest about why.
