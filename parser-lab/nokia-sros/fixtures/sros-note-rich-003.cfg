# synthetic fixture
# no customer data
# purpose: SR OS note-rich parser prep with management-plane hints and policy/security markers
# feature coverage tags: system-identity, interfaces, routed-interface, static-route, management-plane, acl-marker, nat-marker, qos-marker, aaa-marker, security-marker, routing-marker
configure system name "sros-notes-003"
configure system login-control idle-timeout 20
configure port 1/1/4
 description "mgmt-access"
 ethernet
  mode access
 exit
exit
configure router Base
 interface "lo0" create
  address 192.0.2.30/32
  loopback
 exit
 interface "core" create
  address 198.51.100.6/30
  port 1/1/4
 exit
 static-route-entry 0.0.0.0/0 next-hop 198.51.100.5
 static-route-entry 203.0.113.0/24 next-hop 198.51.100.9 preference 20
exit
configure system snmp
 admin-state enable
exit
configure system security ssh
 admin-state enable
exit
configure system time ntp
 server 203.0.113.53
exit
# note-only: ACL markers would include ip-filter and mac-filter objects.
# note-only: NAT markers would include NAT service constructs.
# note-only: QoS markers would include class and queue policies.
# note-only: AAA markers would include authentication and user blocks.
# note-only: security markers would include filtering and service-policy hints.
# note-only: routing markers would include OSPF, BGP, and ISIS families.

