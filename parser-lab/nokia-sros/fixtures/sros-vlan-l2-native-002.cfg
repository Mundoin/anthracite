# synthetic fixture
# no customer data
# purpose: SR OS VLAN/L2 native handling and routed bridge termination parser prep
# feature coverage tags: vlan-l2, sap, native, routed-interface, bvi-like-termination, static-route, note-only markers
configure system name "sros-l2-002"
configure port 1/1/2
 description "access-users"
 ethernet
  mode access
 exit
exit
configure port 1/1/3
 description "trunk-uplink"
 ethernet
  mode network
 exit
exit
configure service vpls 100 customer 1 create
 description "users-vpls"
 sap 1/1/2:100 create
 sap 1/1/3:200 create
 sap 1/1/3 create
 exit
configure router Base
 interface "irb100" create
  address 192.0.2.100/24
  loopback
 exit
 static-route-entry 192.0.2.0/24 next-hop 198.51.100.1
exit
# note-only: ACL markers would include ip-filter and mac-filter objects.
# note-only: NAT markers would include service NAT or translation policies.
# note-only: QoS markers would include forwarding classes and queue policies.
# note-only: AAA markers would include authentication and user management.
# note-only: security markers would include zone or filtering policy objects.
# note-only: routing markers would include protocol families and policies.

