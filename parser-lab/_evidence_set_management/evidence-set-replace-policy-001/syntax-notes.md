# Syntax Notes

- synthetic evidence-set import payloads
- replace means current set becomes the new active_set when the import succeeds
- no_store_mutation must apply on malformed or empty imports
- source_label must remain visible

Vendor note: synthetic evidence-set management syntax.
