# Syntax Notes

- synthetic merge payloads
- merge combines compatible evidence but keeps conflicts explicit
- conflicting_remote_endpoint is preserved, not overwritten
- exactly matching rows may collapse

Vendor note: synthetic evidence-set management syntax.
