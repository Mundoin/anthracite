# Syntax Notes

- synthetic current-view payloads
- current view shows the active_set only
- history remains visible separately
- inspection must not mutate the store

Vendor note: synthetic evidence-set management syntax.
