# Syntax Notes

- EOS CDP detail remains synthetic and conservative in this corpus; treat the shape as illustrative evidence payload rather than production-ready parser truth.
- If the command shape differs in real devices, OCC should keep the pack as a behavioural reference only.
- Exact inventory resolution is still required before any edge is accepted.
- CDP text alone must never become a live edge.

Vendor note: 
illustrative synthetic evidence payload; conservative CDP source.
