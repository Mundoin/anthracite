# Syntax Notes

- This pack uses synthetic import-summary output rather than device command output.
- It is intended to document how V1AP should report parsed, accepted, rejected, and stored counts.
- No topology inference happens here; this is summary-only prep.
- Exact inventory matching remains a prerequisite for any accepted item.

Vendor note: 
synthetic import summary bundle.
