# Syntax Notes

- The pack documents only exact-resolution behaviour.
- Case trimming may be allowed if OCC chooses it, but contains matching stays out-of-scope.
- Chassis ID alone must not invent a node.
- No subnet or VLAN inference.

Vendor note: 
exact inventory resolution only; no fuzzy matching.
