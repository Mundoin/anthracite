# Syntax Notes

- The pack demonstrates output where one or more expected fields are absent.
- The resolver must stay exact-only and conservative.
- Partial data never becomes a topology edge by itself.
- No fuzzy matching and no subnet inference.

Vendor note: 
synthetic partial-field raw output.
