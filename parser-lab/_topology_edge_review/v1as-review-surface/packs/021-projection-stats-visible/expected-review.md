# Expected Review

- The stats strip is visible and reads from projectionStats and evidenceStats.
- Counts are display-only and derived from store truth.
- The operator can read accepted, rejected, conflicting, stale, and duplicate-collapsed summary values without opening the inspector.
