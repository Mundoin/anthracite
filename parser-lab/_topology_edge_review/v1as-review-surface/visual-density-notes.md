# Visual Density Notes

- Keep the table readable when many edges are visible.
- Preserve stable sorting and grouping so the operator can scan the list without the order shifting unexpectedly.
- Selected-edge inspector should stay readable even when the table is dense.
- Do not replace dense review with a graph renderer, canvas, or animated layout.
- Wrap long evidence notes safely, but do not change identity or evidence truth.
