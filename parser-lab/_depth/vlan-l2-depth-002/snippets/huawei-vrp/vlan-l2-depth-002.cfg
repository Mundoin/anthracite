! synthetic fixture snippet
! vendor: Huawei
! platform: vrp
! feature tags: vlan_batch, access_vlan, trunk_native_vlan, trunk_allowed_vlans, svi, spanning_tree_hint, note_only_security
! source: synthetic
! integration_status: not_integrated
system-view
sysname vrp-l2-002
#
vlan batch 10 20 99 100
#
interface GigabitEthernet0/0/1
 description access-users
 port link-type access
 port default vlan 10
 stp edged-port enable
 undo shutdown
#
interface GigabitEthernet0/0/2
 description trunk-uplink
 port link-type trunk
 port trunk allow-pass vlan 10 20 99 100
 port trunk pvid vlan 99
 undo shutdown
#
interface Vlanif99
 description mgmt-svi
 ip address 192.0.2.99 255.255.255.0
 undo shutdown
#
return
# note-only: acl / nat / qos / aaa / security / routing markers stay conservative.

