# synthetic fixture snippet
# vendor: MikroTik
# platform: routeros
# feature tags: ssh, snmp, ntp, logging, aaa, local_users, note_only_security, note_only_routing
# source: synthetic
# integration_status: not_integrated
/system identity set name=ros-mgmt-005
/ip service set ssh disabled=no port=22
/snmp set enabled=yes
/system ntp client set enabled=yes primary-ntp=192.0.2.53
/user add name=admin group=full password=fake-hash
/user group add name=ops policy=read,write,ssh
# note-only: acl / nat / qos / security / routing markers stay conservative.

