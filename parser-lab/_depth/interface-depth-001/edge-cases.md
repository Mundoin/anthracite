# interface-depth-001 edge cases

- Loopback naming differs across vendors.
- Huawei uses `undo shutdown` instead of `no shutdown`.
- RouterOS renames interfaces before addressing them.
- FortiOS interface IP syntax is `address + netmask` style.
- SR OS separates `configure port` and `configure router Base`.
- Note-only management/security lines should stay conservative.

