! synthetic fixture snippet
! vendor: Fortinet
! platform: fortios
! feature tags: system_global, hostname, interfaces, routed_interface, ipv4, management_hint, note_only_security
! source: synthetic
! integration_status: not_integrated
config system global
    set hostname "fg-intf-001"
end
config system interface
    edit "port1"
        set alias "wan-uplink"
        set ip 198.51.100.18 255.255.255.252
        set allowaccess ping ssh
        set role wan
    next
    edit "port2"
        set alias "lan-uplink"
        set ip 192.0.2.18 255.255.255.0
        set allowaccess ping https
        set role lan
    next
end
# note-only: acl / nat / qos / aaa / security / routing markers stay conservative.

