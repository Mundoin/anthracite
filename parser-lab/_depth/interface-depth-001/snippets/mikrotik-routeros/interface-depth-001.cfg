# synthetic fixture snippet
# vendor: MikroTik
# platform: routeros
# feature tags: system_identity, bridge, routed_interface, ipv4, management_hint, note_only_security
# source: synthetic
# integration_status: not_integrated
/system identity set name=ros-intf-001
/interface ethernet set [ find default-name=ether1 ] name=wan1
/interface ethernet set [ find default-name=ether2 ] name=lan1
/interface bridge add name=bridge-lan vlan-filtering=yes
/ip address add address=198.51.100.14/30 interface=wan1
/ip address add address=192.0.2.1/24 interface=bridge-lan
/ip service set ssh disabled=no
/snmp set enabled=yes
# note-only: acl / nat / qos / aaa / security / routing markers stay conservative.

