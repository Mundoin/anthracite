# synthetic fixture snippet
# vendor: Nokia
# platform: sros
# feature tags: system_identity, loopback, routed_interface, ipv4, management_hint, note_only_security
# source: synthetic
# integration_status: not_integrated
configure system name "sros-intf-001"
configure port 1/1/1
 description "wan-uplink"
 ethernet
  mode network
 exit
exit
configure router Base
 interface "lo0" create
  address 192.0.2.4/32
  loopback
 exit
 interface "to-wan" create
  address 198.51.100.22/30
  port 1/1/1
 exit
exit
# note-only: acl / nat / qos / aaa / security / routing markers stay conservative.

