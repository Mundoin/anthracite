! synthetic fixture snippet
! vendor: Fortinet
! platform: fortios
! feature tags: static_routes, default_route, prefix_route, device_binding, note_only_security
! source: synthetic
! integration_status: not_integrated
config router static
    edit 1
        set dst 0.0.0.0/0
        set gateway 198.51.100.1
        set device "port1"
    next
    edit 2
        set dst 192.0.2.0/24
        set gateway 198.51.100.3
        set device "port2"
        set distance 10
    next
end
# note-only: acl / nat / qos / aaa / security / routing markers stay conservative.

