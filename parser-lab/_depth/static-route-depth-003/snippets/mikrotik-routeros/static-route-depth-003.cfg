# synthetic fixture snippet
# vendor: MikroTik
# platform: routeros
# feature tags: static_routes, default_route, prefix_route, distance, note_only_security
# source: synthetic
# integration_status: not_integrated
/system identity set name=ros-route-001
/ip route add dst-address=0.0.0.0/0 gateway=198.51.100.1
/ip route add dst-address=203.0.113.0/24 gateway=203.0.113.1 distance=10
/ip route add dst-address=192.0.2.0/24 gateway=192.0.2.1 distance=20
# note-only: acl / nat / qos / aaa / security / routing markers stay conservative.

