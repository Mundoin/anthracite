# mikrotik-routeros static-route-depth-003 intent

## Extraction expectations

- Default route.
- Prefix routes with distance values.

## Conservative areas

- Preserve `dst-address` and `gateway` fields as written.
- Do not infer dynamic routing behavior.

