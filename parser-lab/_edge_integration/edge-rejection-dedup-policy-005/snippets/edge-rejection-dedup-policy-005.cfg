! synthetic fixture
! no customer data
! purpose: dedup and rejection policy examples
! feature tags: dedup, rejection, topology-link-fact
! source: synthetic
! integration_status: not_integrated
! note-only: keep one fact per real edge and reject weak claims explicitly.

! === dedup-iosxe-001 | vendor=Cisco | platform=IOS-XE ===
hostname dedup-iosxe-001
lldp run
cdp run
interface GigabitEthernet0/0/0
 description uplink-to-spine-a
 no shutdown
! lldp payload:
! local_node: dedup-iosxe-001
! local_interface: GigabitEthernet0/0/0
! remote_node: spine-a-01
! remote_interface: Ethernet1/49
! evidence_kind: lldp
! evidence_source: discovery
! confidence: high
! cdp payload:
! local_node: dedup-iosxe-001
! local_interface: GigabitEthernet0/0/0
! remote_node: spine-a-01
! remote_interface: Ethernet1/49
! evidence_kind: cdp
! evidence_source: discovery
! confidence: high
! mapping expectation:
! record_state: merged
! dedup_key: canonical endpoint pair
! notes: keep both evidence bundles, one fact

! === dedup-junos-002 | vendor=Juniper | platform=Junos ===
system {
    host-name dedup-junos-002;
}
protocols {
    lldp {
        interface all;
    }
}
interfaces {
    ge-0/0/0 {
        unit 0 {
            family inet {
                address 198.51.100.1/30;
            }
        }
    }
}
! lldp payload:
! local_node: dedup-junos-002
! local_interface: ge-0/0/0.0
! remote_chassis_id: 00aa.bbcc.ddee
! remote_port_id: xe-0/0/48
! remote_system_name: unknown
! mapping expectation:
! record_state: rejected
! reject_reason: unknown_remote_node
! notes: keep the raw payload for later correlation, but do not accept an edge

! === dedup-eos-003 | vendor=Arista | platform=EOS ===
hostname dedup-eos-003
interface Port-Channel1000
   description mlag-peer-link
   switchport mode trunk
   no shutdown
mlag configuration
   domain-id DC1-MLAG
   local-interface Vlan4094
   peer-address 10.255.255.2
   peer-link Port-Channel1000
! manual note:
! peer identity was entered incorrectly and resolves to the local node
! mapping expectation:
! record_state: rejected
! reject_reason: self_link
! notes: keep the rejection reason, do not promote the candidate
