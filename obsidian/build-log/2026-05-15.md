# 2026-05-15 — Ground zero scaffold

Session: V1A scaffold landed by Claude (Opus 4.7, 1M context).

- Created Tauri 2 + React + TypeScript + Vite skeleton.
- Installed `@babylonjs/core` and `@babylonjs/loaders` as runtime deps.
- Added dark cockpit layout (title bar + 4 panels + center Babylon canvas).
- Wrote scaffold Rust backend (`anthracite_lib::run` + `ping` command).
- Docs: README, PRODUCT, GOALS, AGENTS, CLAUDE.
- Obsidian vault skeleton with index, stage note, stack-lock decision.
- PowerShell automation: `tools/status.ps1`, `tools/validate.ps1`.
- No code copied from `_NEXUS`. No Python introduced.

Validation: see Claude's V1A report for `pnpm typecheck`, `pnpm build`,
`cargo check`, `tools/validate.ps1` output.
